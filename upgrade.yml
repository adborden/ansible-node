---
- name: Upgrade
  hosts: all
  tasks:
    - name: Upgrade os packages
      ansible.builtin.apt:
        upgrade: dist
        autoclean: "yes"
        autoremove: "yes"
        cache_valid_time: "{{ 12 * 3600 }}"
- name: Reboot
  import_playbook: reboot.yml
