---
- name: Prepare
  hosts: all
  tasks:
    - name: Install assumed os packages
      ansible.builtin.apt:
        package: "{{ packages }}"
        update_cache: true
      vars:
        packages:
          - tzdata
